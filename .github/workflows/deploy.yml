name: Deploy

on:
  push:
    branches: [main]

jobs:
  deploy:
    # Deploy workflow is suspended pending matchserver re-implementation.
    # See: Refactor commit 36aa4aa
    name: Deploy to VPS (suspended)
    runs-on: ubuntu-latest
    steps:
      - name: Deploy suspended
        run: echo "Matchserver deploy is currently suspended."
