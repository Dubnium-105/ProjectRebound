name: CI

on:
  push:
    branches: ["**"]
  pull_request:
    branches: [main]

jobs:
  dotnet-build:
    name: .NET Build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-dotnet@v4
        with:
          dotnet-version: "8.0.x"

      - name: Build Contracts
        run: dotnet build Shared/ProjectRebound.Contracts/ProjectRebound.Contracts.csproj --configuration Release

      - name: Build Browser
        run: dotnet build Desktop/ProjectRebound.Browser/ProjectRebound.Browser.csproj --configuration Release
